# Copy to .env (git-ignored) and fill in real values. Never commit .env.
DB_HOST=localhost
DB_NAME=medreach
DB_USER=root
DB_PASS=

# Email delivery for sign-up / password-reset codes
RESEND_API_KEY=
MAIL_FROM="MedReach <no-reply@example.com>"
